# Versions used for the RQ6 / RQ8 runs (Python 3.11). Full frozen environment of the
# CheXpert sweep is in configs/rq8/shift50_v1/environment.txt.
torch==2.5.1
torchvision==0.20.1
numpy==2.3.3
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.15.0
matplotlib==3.11.1
seaborn==0.13.2
joblib>=1.3
Pillow==12.3.0
PyYAML==6.0.3
tqdm==4.70.0
# optional: only needed for method families / datasets not used in the paper's RQ6-RQ8 tables
# concept-erasure==0.2.4   (LEACE eraser branch in experiments/rq8_final/train.py)
# folktables              (ACSIncome loaders in the shared RQ5 data module)
# wfdb                    (PTB-XL waveform loading in the shared RQ4 module)
